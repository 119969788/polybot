# 📤 上传诊断工具到服务器

## 问题

服务器上缺少 `diagnose-failures.js` 文件。

## 解决方案

### 方法一：使用 SCP 上传（推荐）

在**本地 Windows 电脑**上执行：

```powershell
# 替换 user@server-ip 为你的服务器信息
# 例如：root@123.456.789.0

# 上传诊断工具
scp diagnose-failures.js user@server-ip:/opt/polybot/

# 上传改进的 WalletFollower.js（如果需要）
scp src/WalletFollower.js user@server-ip:/opt/polybot/src/

# 上传排查脚本（可选）
scp 快速排查跟单失败.sh user@server-ip:/opt/polybot/

# 上传更新的 package.json（如果需要）
scp package.json user@server-ip:/opt/polybot/
```

### 方法二：使用 Git（如果使用版本控制）

**本地提交代码**：

```bash
git add diagnose-failures.js
git add src/WalletFollower.js
git add package.json
git commit -m "添加诊断工具和改进错误处理"
git push origin main
```

**服务器拉取**：

```bash
# SSH 连接到服务器
ssh user@server-ip
cd /opt/polybot
git pull origin main
```

### 方法三：手动创建文件（直接在服务器上）

如果无法上传，可以直接在服务器上创建文件：

```bash
# SSH 连接到服务器
ssh user@server-ip
cd /opt/polybot

# 使用 cat 创建文件（复制下面的内容）
cat > diagnose-failures.js << 'DIAGNOSTIC_EOF'
[文件内容]
DIAGNOSTIC_EOF
```

## 验证上传

上传后，在服务器上验证：

```bash
# 检查文件是否存在
ls -la diagnose-failures.js

# 验证语法
node --check diagnose-failures.js

# 运行诊断
node diagnose-failures.js
```

## 快速命令（一行上传）

```powershell
# Windows PowerShell（在本地执行）
scp diagnose-failures.js user@server-ip:/opt/polybot/ && echo "✅ 上传成功"
```

## 如果使用 Git

```bash
# 本地提交
git add . && git commit -m "添加诊断工具" && git push origin main

# 服务器拉取
ssh user@server-ip "cd /opt/polybot && git pull origin main"
```

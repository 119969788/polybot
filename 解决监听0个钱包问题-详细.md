# 🔧 解决"正在监听 0 个钱包"问题（详细版）

## 问题分析

根据输出：
```
📊 获取前 10 名顶级交易者...
✅ 成功加载 10 名交易者
✅ 初始化完成，正在监听 0 个钱包
⚠️  没有要监听的钱包，请先添加钱包
```

**问题**：虽然成功加载了 10 名交易者，但最终监听 0 个钱包。

## 可能的原因

### 1. 所有交易者都被过滤条件过滤掉了

**现象**：
- 显示了"✅ 成功加载 10 名交易者"
- 但没有显示"通过过滤: X 个"

**原因**：
- 配置的过滤条件太严格（`minWinRate`、`minSmartScore`）
- 所有交易者都不满足条件

**解决方案**：

检查 `config.js` 中的过滤条件：

```javascript
filters: {
  // minWinRate: 0,  // ✅ 注释掉或设置为 0（不限制）
  // minSmartScore: 0,  // ✅ 注释掉或设置为 0（不限制）
  excludeTokens: [],
}
```

**快速修复**：

```bash
cd /opt/polybot
nano config.js
```

确保 `filters` 部分如下：

```javascript
filters: {
  // 全部注释掉，不设置任何过滤条件
  excludeTokens: [],
}
```

### 2. 获取钱包资料失败

**现象**：
- 可能在日志中看到错误信息
- 钱包地址无效或未注册

**解决方案**：

检查日志中是否有错误信息：
- `⚠️  获取钱包 ... 资料失败`
- `❌ 处理交易者 ... 时出错`

如果看到这些错误，可能是：
1. 钱包地址无效
2. 钱包未在 Polymarket 注册
3. 网络连接问题

### 3. targetWallets 配置的钱包无法添加

**现象**：
- 配置了 `targetWallets`，但仍显示 0 个钱包

**可能原因**：
1. 钱包地址格式错误
2. 钱包未在 Polymarket 注册
3. API 调用失败

**解决方案**：

检查 `targetWallets` 配置：

```javascript
targetWallets: [
  '0xe00740bce98a594e26861838885ab310ec3b548c',
  '0x6031b6eed1c97e853c6e0f03ad3ce3529351f96d',
],
```

确保：
- 地址格式正确（42 个字符，包含 `0x` 前缀）
- 钱包地址是有效的以太坊地址
- 钱包在 Polymarket 上有活动

### 4. 从排行榜获取的交易者都被过滤

**现象**：
- "✅ 成功加载 10 名交易者"
- 但没有显示"通过过滤: X 个"

**解决方案**：

临时禁用所有过滤条件：

```javascript
filters: {
  // 全部注释掉
  excludeTokens: [],
}
```

## 诊断步骤

### 步骤 1: 检查配置

```bash
cd /opt/polybot

# 检查过滤条件
grep -A 5 "filters:" config.js

# 检查目标钱包
grep -A 5 "targetWallets:" config.js
```

### 步骤 2: 查看详细日志

运行程序并查看详细输出：

```bash
cd /opt/polybot
npm start 2>&1 | tee output.log

# 查看日志
cat output.log | grep -i "过滤\|添加\|钱包\|错误"
```

### 步骤 3: 检查是否显示了"通过过滤"信息

如果程序显示：
```
✅ 成功加载 10 名交易者
   通过过滤: 0 个
   被过滤: 10 个（可能因为胜率或评分不达标）
```

说明所有交易者都被过滤掉了，需要禁用过滤条件。

### 步骤 4: 检查钱包地址格式

```bash
# 检查钱包地址格式
cd /opt/polybot
node -e "
const config = require('./config.js').default;
if (config.targetWallets) {
  config.targetWallets.forEach(addr => {
    console.log('地址:', addr);
    console.log('长度:', addr.length);
    console.log('格式:', addr.startsWith('0x') && addr.length === 42 ? '✅ 正确' : '❌ 错误');
  });
}
"
```

## 快速修复方案

### 方案一：禁用所有过滤条件（推荐）

```bash
cd /opt/polybot
nano config.js
```

修改 `filters` 部分：

```javascript
filters: {
  // 全部注释掉，不设置任何过滤条件
  // minWinRate: 0,
  // minSmartScore: 0,
  excludeTokens: [],
}
```

### 方案二：使用 sed 快速修复

```bash
cd /opt/polybot

# 备份配置
cp config.js config.js.bak

# 注释掉过滤条件（如果存在）
sed -i "s/minWinRate:/\/\/ minWinRate:/g" config.js
sed -i "s/minSmartScore:/\/\/ minSmartScore:/g" config.js

# 验证
grep -A 5 "filters:" config.js
```

### 方案三：使用已知有效的钱包地址

```javascript
targetWallets: [
  '0xe00740bce98a594e26861838885ab310ec3b548c',
  '0x6031b6eed1c97e853c6e0f03ad3ce3529351f96d',
],
```

## 验证修复

修复后重新运行：

```bash
cd /opt/polybot
npm start
```

应该看到：
```
✅ 成功加载 10 名交易者
   通过过滤: X 个（X > 0）
✅ 初始化完成，正在监听 X 个钱包（X > 0）
```

或者：
```
✅ 初始化完成，正在监听 2 个钱包（如果使用 targetWallets）
```

## 推荐配置（确保钱包能添加）

```javascript
export default {
  sdk: {
    privateKey: process.env.POLYMARKET_PRIVATE_KEY || '你的私钥',
    chainId: 137,
  },
  targetWallets: [
    '0xe00740bce98a594e26861838885ab310ec3b548c',
    '0x6031b6eed1c97e853c6e0f03ad3ce3529351f96d',
  ],
  topTradersCount: 10,
  followSettings: {
    autoFollow: true,
    dryRun: true,
    sizeScale: 0.1,
    maxSizePerTrade: 10,
    minTradeSize: 1,
    maxSlippage: 0.05,
    orderType: 'FAK',
  },
  filters: {
    // ✅ 全部注释掉，不设置任何过滤条件
    excludeTokens: [],
  }
};
```

## 如果仍然显示 0 个钱包

1. **检查网络连接**：
   ```bash
   ping polymarket.com
   ```

2. **检查 SDK 是否正常工作**：
   ```bash
   npm run diagnose
   ```

3. **查看详细错误日志**：
   ```bash
   npm start 2>&1 | tee debug.log
   cat debug.log | grep -i "error\|失败\|filter"
   ```

4. **尝试使用环境变量配置代理**（如果在中国大陆）：
   ```bash
   export HTTP_PROXY=http://127.0.0.1:7890
   export HTTPS_PROXY=http://127.0.0.1:7890
   npm start
   ```

---

**修复后应该能看到 "正在监听 X 个钱包（X > 0）"！**

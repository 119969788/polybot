# 🔍 腾讯云服务器配置检查指南

## 根据您的输出分析

从您的输出看到：
- ✅ 程序正常运行
- ❌ 只读模式（私钥未找到）
- ❌ 正在监听 0 个钱包
- ❌ 自动跟单已禁用

## 问题诊断

### 问题 1：只读模式

**原因**：服务器上的 `config.js` 可能：
1. 私钥为空或未配置
2. 从 GitHub 克隆时使用的是 `config.example.js`
3. 私钥被 `.gitignore` 排除了

**检查方法**：

```bash
# SSH 连接到服务器
ssh user@your-server-ip

# 进入项目目录
cd /opt/polybot  # 或您的项目路径

# 检查配置文件
cat config.js | grep -A 5 "privateKey"

# 如果显示空或注释，需要配置私钥
```

### 问题 2：正在监听 0 个钱包

**原因**：
1. `targetWallets` 列表为空
2. 钱包资料获取失败（网络问题）
3. 钱包被过滤条件过滤掉

**检查方法**：

```bash
# 检查目标钱包配置
cat config.js | grep -A 10 "targetWallets"

# 应该看到：
# targetWallets: [
#   '0xe00740bce98a594e26861838885ab310ec3b548c',
#   '0x6031b6eed1c97e853c6e0f03ad3ce3529351f96d',
# ],
```

### 问题 3：自动跟单已禁用

**原因**：`config.js` 中的 `autoFollow` 可能是 `false` 或未设置

**检查方法**：

```bash
# 检查自动跟单配置
cat config.js | grep -A 5 "autoFollow"

# 应该看到：
# autoFollow: true,
```

## 快速修复方案

### 方案一：直接修改 config.js（最简单）

在服务器上执行：

```bash
cd /opt/polybot  # 替换为您的项目路径

# 备份当前配置
cp config.js config.js.backup

# 编辑配置文件
nano config.js
```

确保以下配置正确：

```javascript
export default {
  sdk: {
    privateKey: '0xd4ae880287b31d8316f31e938a4bb50d6260d765229076be83d8fa7962f2531b',
    chainId: 137,
  },
  targetWallets: [
    '0xe00740bce98a594e26861838885ab310ec3b548c',
    '0x6031b6eed1c97e853c6e0f03ad3ce3529351f96d',
  ],
  topTradersCount: 2,  // 或者改为 10
  followSettings: {
    sizeScale: 0.1,
    maxSizePerTrade: 10,
    minTradeSize: 1,
    maxSlippage: 0.03,
    orderType: 'FOK',
    autoFollow: true,   // ⚠️ 必须是 true，不要引号
    dryRun: false,      // 真实交易模式
    watchInterval: 5000,
  },
  filters: {
    // 不设置过滤条件，确保钱包不被过滤
    // minWinRate: undefined,
    // minSmartScore: undefined,
    excludeTokens: [],
  }
};
```

### 方案二：使用环境变量配置私钥（推荐）

```bash
# 编辑 ~/.bashrc
nano ~/.bashrc

# 添加以下行
export POLYMARKET_PRIVATE_KEY="0xd4ae880287b31d8316f31e938a4bb50d6260d765229076be83d8fa7962f2531b"

# 使配置生效
source ~/.bashrc

# 验证
echo $POLYMARKET_PRIVATE_KEY
```

然后在 `config.js` 中：

```javascript
sdk: {
  privateKey: process.env.POLYMARKET_PRIVATE_KEY || '',
  chainId: 137,
},
```

## 完整的服务器配置检查脚本

创建一个检查脚本 `check-config.sh`：

```bash
#!/bin/bash

echo "========================================"
echo "  配置检查脚本"
echo "========================================"
echo ""

# 检查配置文件
if [ -f "config.js" ]; then
    echo "✅ config.js 存在"
    
    # 检查私钥
    if grep -q "privateKey:.*0x" config.js; then
        echo "✅ 私钥已配置"
    else
        echo "❌ 私钥未配置"
        echo "   请在 config.js 中配置 privateKey"
    fi
    
    # 检查 autoFollow
    if grep -q "autoFollow:.*true" config.js; then
        echo "✅ autoFollow 已启用"
    else
        echo "❌ autoFollow 未启用或为 false"
        echo "   请在 config.js 中设置 autoFollow: true"
    fi
    
    # 检查 targetWallets
    if grep -q "targetWallets:" config.js && grep -A 5 "targetWallets:" config.js | grep -q "0x"; then
        echo "✅ targetWallets 已配置"
        echo "   钱包地址："
        grep -A 5 "targetWallets:" config.js | grep "0x" | sed 's/^/      /'
    else
        echo "❌ targetWallets 未配置或为空"
    fi
else
    echo "❌ config.js 不存在"
    echo "   请从 config.example.js 复制创建"
fi

echo ""
echo "检查环境变量..."
if [ -n "$POLYMARKET_PRIVATE_KEY" ]; then
    echo "✅ POLYMARKET_PRIVATE_KEY 环境变量已设置"
    echo "   长度: ${#POLYMARKET_PRIVATE_KEY} 字符"
else
    echo "❌ POLYMARKET_PRIVATE_KEY 环境变量未设置"
fi

echo ""
echo "测试网络连接..."
if curl -s --connect-timeout 5 https://polymarket.com > /dev/null; then
    echo "✅ 可以访问 Polymarket"
else
    echo "❌ 无法访问 Polymarket（可能需要代理）"
fi

echo ""
echo "========================================"
```

## 运行检查脚本

```bash
# 创建脚本
cat > check-config.sh << 'EOF'
# 上面的脚本内容
EOF

# 添加执行权限
chmod +x check-config.sh

# 运行检查
./check-config.sh
```

## 修复后的验证

修复配置后，重新运行程序应该看到：

```
✅ poly-sdk 初始化成功（交易模式）
📋 配置检查:
   自动跟单: ✅ 已启用
   私钥状态: ✅ 已配置 (66 字符)
   交易服务: ✅ 可用
   测试模式: ❌ 否
✅ 初始化完成，正在监听 2 个钱包
🎯 使用完整的自动跟单功能（SmartMoneyService）...
```

## 如果仍然有问题

### 网络问题

如果腾讯云服务器无法访问 Polymarket API：

1. **配置代理**（如果有）：
   ```bash
   export HTTP_PROXY="http://代理地址:端口"
   export HTTPS_PROXY="http://代理地址:端口"
   npm start
   ```

2. **或者使用本地测试**：
   - 在本地 Windows 环境运行（可以使用代理）
   - 服务器仅用于生产环境部署

### 钱包未注册

如果显示"钱包未在 Polymarket 注册"：
1. 访问 https://polymarket.com/
2. 使用私钥对应的钱包连接
3. 完成注册流程

---

**请按照以上步骤检查并修复服务器配置，然后重新运行程序！**

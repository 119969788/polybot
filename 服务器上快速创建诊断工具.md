# 🔧 在服务器上快速创建诊断工具

## 最简单的方法

如果无法上传文件，可以直接在服务器上创建。

### 步骤 1：SSH 连接到服务器

```bash
ssh user@your-server-ip
cd /opt/polybot
```

### 步骤 2：使用 wget 或 curl 下载（如果有公开 URL）

如果文件已提交到 Git 仓库：

```bash
# 如果使用 GitHub
wget https://raw.githubusercontent.com/your-username/polybot/main/diagnose-failures.js
# 或
curl -O https://raw.githubusercontent.com/your-username/polybot/main/diagnose-failures.js
```

### 步骤 3：使用 cat 手动创建（推荐）

```bash
cat > diagnose-failures.js << 'EOF'
[将文件内容复制粘贴到这里]
EOF
```

**或者使用 nano 编辑器**：

```bash
nano diagnose-failures.js
# 粘贴文件内容，然后按 Ctrl+X, Y, Enter 保存
```

### 步骤 4：验证和运行

```bash
# 检查文件
ls -lh diagnose-failures.js

# 验证语法
node --check diagnose-failures.js

# 运行诊断
node diagnose-failures.js
```

## 使用 Git（如果使用版本控制）

**最简单的方法**：

```bash
# 在本地提交代码
git add diagnose-failures.js
git commit -m "添加诊断工具"
git push origin main

# 在服务器上拉取
ssh user@server-ip
cd /opt/polybot
git pull origin main
node diagnose-failures.js
```

## 临时解决方案：简化版诊断

如果无法创建完整文件，可以先创建一个简化版：

```bash
cat > quick-check.js << 'EOF'
import config from './config.js';

console.log('📋 配置检查:');
const s = config.followSettings || {};
console.log('订单类型:', s.orderType || 'FOK');
console.log('滑点:', (s.maxSlippage || 0.03) * 100 + '%');
console.log('最大金额: $' + (s.maxSizePerTrade || 10));
console.log('测试模式:', s.dryRun !== false ? '是' : '否');
EOF

node quick-check.js
```

---

**建议：使用 Git 是最简单可靠的方法！**

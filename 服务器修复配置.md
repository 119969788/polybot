# 🔧 服务器配置修复指南

根据诊断结果，需要在服务器上修复以下配置：

## 问题清单

1. ❌ `autoFollow` 未启用
2. ⚠️ 未配置私钥
3. 💡 建议明确设置 `orderType: "FAK"`

## 快速修复步骤

### 方法一：直接编辑 config.js（推荐）

```bash
cd /opt/polybot
nano config.js
```

需要修改的内容：

#### 1. 确保 `followSettings.autoFollow` 设置为 `true`

找到 `followSettings` 部分，确保：
```javascript
followSettings: {
  // ... 其他配置 ...
  autoFollow: true,  // ✅ 必须是 true
  // ... 其他配置 ...
}
```

#### 2. 确保私钥已配置

找到 `sdk` 部分，确保：
```javascript
sdk: {
  privateKey: process.env.POLYMARKET_PRIVATE_KEY || '你的私钥',  // ✅ 必须配置
  chainId: 137,
}
```

#### 3. 确保 `orderType` 设置为 `'FAK'`

在 `followSettings` 中：
```javascript
followSettings: {
  // ... 其他配置 ...
  orderType: 'FAK',  // ✅ 设置为 'FAK'
  // ... 其他配置 ...
}
```

### 方法二：使用 sed 命令快速修复

```bash
cd /opt/polybot

# 备份配置文件
cp config.js config.js.bak

# 确保 autoFollow 为 true（如果当前是 false）
sed -i "s/autoFollow: false/autoFollow: true/g" config.js

# 确保 orderType 为 'FAK'（如果当前是 'FOK'）
sed -i "s/orderType: 'FOK'/orderType: 'FAK'/g" config.js
sed -i 's/orderType: "FOK"/orderType: "FAK"/g' config.js

# 验证修复
grep "autoFollow:" config.js
grep "orderType:" config.js
```

### 方法三：完整的推荐配置

```bash
cd /opt/polybot
nano config.js
```

复制以下完整配置（替换 `followSettings` 部分）：

```javascript
followSettings: {
  // 跟单比例（0-1之间，例如 0.1 表示跟单10%的交易量）
  sizeScale: 0.1,
  
  // 最大单笔跟单金额（美元）
  maxSizePerTrade: 10,
  
  // 最小单笔跟单金额（美元）
  minTradeSize: 1,  // Polymarket 最小订单是 $1
  
  // 滑点容忍度（0-1之间，例如 0.05 表示5%）
  maxSlippage: 0.05,  // 5% 滑点
  
  // 订单类型：'FAK'（部分成交也可以）
  orderType: 'FAK',  // 允许部分成交，提高成功率
  
  // 是否启用自动跟单
  autoFollow: true,  // ✅ 必须为 true
  
  // 测试模式（dry run）
  dryRun: true,  // ✅ 测试模式 - 不会执行真实交易
  
  // 监听间隔（毫秒）
  watchInterval: 5000,
},
```

## 关于私钥配置

### 方式一：使用环境变量（推荐，更安全）

```bash
# 在服务器上设置环境变量
export POLYMARKET_PRIVATE_KEY="0x你的私钥"

# 在 config.js 中：
sdk: {
  privateKey: process.env.POLYMARKET_PRIVATE_KEY || '',
  chainId: 137,
}
```

### 方式二：直接在 config.js 中配置（简单但不推荐）

```javascript
sdk: {
  privateKey: '0x你的私钥',  // ⚠️ 注意安全
  chainId: 137,
}
```

## 验证修复

修复后，运行诊断工具验证：

```bash
cd /opt/polybot
npm run diagnose
# 或
node diagnose-failures.js
```

应该看到：
```
✅ 自动跟单已启用
✅ 私钥已配置
✅ 订单类型: FAK
```

## 配置检查清单

- [ ] `followSettings.autoFollow` 设置为 `true`
- [ ] `followSettings.orderType` 设置为 `'FAK'`
- [ ] `followSettings.maxSlippage` 设置为 `0.05` 或更高
- [ ] `sdk.privateKey` 已配置（环境变量或直接配置）
- [ ] `followSettings.dryRun` 设置为 `true`（测试模式）

## 完整推荐配置示例

```javascript
export default {
  sdk: {
    privateKey: process.env.POLYMARKET_PRIVATE_KEY || '你的私钥',
    chainId: 137,
  },
  targetWallets: [
    '0xe00740bce98a594e26861838885ab310ec3b548c',
    '0x6031b6eed1c97e853c6e0f03ad3ce3529351f96d',
  ],
  topTradersCount: 10,
  followSettings: {
    sizeScale: 0.1,
    maxSizePerTrade: 10,
    minTradeSize: 1,
    maxSlippage: 0.05,
    orderType: 'FAK',
    autoFollow: true,
    dryRun: true,
    watchInterval: 5000,
  },
  filters: {
    excludeTokens: [],
  }
};
```

---

**修复后重新运行诊断工具确认所有问题已解决！**
